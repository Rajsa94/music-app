import 'package:flutter/material.dart';
import 'colors.dart';

ourStyle({family = "Open Sans", double size = 14, color = bgColor}) {
  return TextStyle(fontSize: size, color: color, fontFamily: family);
}
